package com.lightricity.station.units.model

import com.lightricity.station.R

enum class SoundUnit (val code: Int, val title: Int, val unit: Int) {
    DB(0, R.string.sound_db_name, R.string.sound_db_unit),
}
