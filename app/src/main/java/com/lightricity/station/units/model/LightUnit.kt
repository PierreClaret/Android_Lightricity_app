package com.lightricity.station.units.model

import com.lightricity.station.R

enum class LightUnit(val code: Int, val title: Int, val unit: Int) {
    LUMEX(0, R.string.light_lum_name, R.string.light_lum_unit),
}